apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: openmpi-api }
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels: { app.kubernetes.io/name: openmpi, app.kubernetes.io/component: api }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: openmpi-mllp }
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels: { app.kubernetes.io/name: openmpi, app.kubernetes.io/component: mllp }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: openmpi-api }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: openmpi-api }
  minReplicas: {{ .Values.autoscaling.api.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.api.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: {{ .Values.autoscaling.api.targetCPUUtilizationPercentage }} }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: openmpi-mllp }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: openmpi-mllp }
  minReplicas: {{ .Values.autoscaling.mllp.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.mllp.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: {{ .Values.autoscaling.mllp.targetCPUUtilizationPercentage }} }
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: openmpi-portal }
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels: { app.kubernetes.io/name: openmpi, app.kubernetes.io/component: portal }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: openmpi-portal }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: openmpi-portal }
  minReplicas: {{ .Values.autoscaling.portal.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.portal.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: {{ .Values.autoscaling.portal.targetCPUUtilizationPercentage }} }
